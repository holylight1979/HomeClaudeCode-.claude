# Credentials & secrets
.credentials.json
mcp-needs-auth-cache.json

# Python cache
__pycache__/
*.pyc

# Vector DB (local, regenerated by indexer)
memory/_vectordb/

# Episodic atoms (TTL 24d, auto-generated, not for version control)
memory/episodic/

# Distant memory archive (expired atoms)
memory/_distant/

# User-specific atoms (hardware, local env, not for shared repo)
memory/hardware.md

# Workflow session states (ephemeral)
workflow/state-*.json

# Cache & generated
cache/
debug/
file-history/
downloads/
shell-snapshots/
telemetry/
todos/
backups/
plans/
ide/
plugins/
stats-cache.json

# Session transcripts (huge, not needed in repo)
projects/*/*.jsonl
projects/*/*/

# Keep only memory/ directories inside projects
!projects/*/memory/
!projects/*/memory/**
